services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 80:80
        restart: always
        volumes: 
            - /home/ubuntu/.microsoft/usersecrets:/app/secrets